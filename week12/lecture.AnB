Protocol: EasyID

Types: Agent A,B;
       Number M;
       Function pk, sk;
       Symmetric_key Ktemp

Knowledge: A: A, pk(A), inv(pk(A));
           B: B, pk(B), inv(pk(B));
#where A != B

Actions:

A *-> B : pk(A)
B -> A : { M }pk(A)

Goals:

B ->* A: M
